<div class="page-header">
  <h1>
    <%= @config.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_config_path(@config), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @config.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @config.created_at %></td>
    </tr>
  </table>
</div>
