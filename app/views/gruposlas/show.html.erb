<div class="page-header">
  <h1>
    <%= @gruposla.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_gruposla_path(@gruposla), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @gruposla.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @gruposla.created_at %></td>
    </tr>
  </table>
</div>
